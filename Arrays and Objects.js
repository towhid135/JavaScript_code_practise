//{Array}

var array = [] // inorder to assign things to an array first needs to create an empty array.
var array = Array();
var ara = Array(1,2,3);

var num = [1,2,3,4];
var num.push(5); //add number to the tail
var.pop(); //pop a number from the tail
var.unshift(100); // add number to the head
var.shift();  //pop number from the head

var.push([1,2,3]);

//{Object}

Obj = {
	'Name': 'Towhid', //property can be written inside quotation
	Id: 17101135,     // property can be written without quotation
	roll: 135 
};

//Access of object

emptyObj = {} //

emptyobj.Id = 17101135; //assigning values to empty array, property can be defined according to users wish

emptyObj2 = Object(); // another way to create empty array.

obj.Name;
obj['Name'];
str = 'Name';
obj[str];

obj.Id;
obj['Id'};

//Combined

var ara = [{
	Name: 'towhid',
	id: 17101135
},100,200,'3rd index'];

ara[0].Name;
ara[0]['Name'];

var obj = {
Color: ['red','green','White'],
id:135
}

obj.Color[0];
