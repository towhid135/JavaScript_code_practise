var num = 10;
a = 20;
b = '20';
var name = 'towhid';


a == b; // true (only checks the value, if equal then it returns true)
a ===b // false ( checks the value and data type)

c = '20'+1;  // ans:'201'
c = (c * 1) + 1; // 202 (becomes integer)

typeof c; // returns the data type of the variable.

